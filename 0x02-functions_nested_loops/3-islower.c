#include "main.h"
/**
 * _islower - return 1 if its lowercase
 * @c: char type letter
 *
 * Return: 1 if its lowercase
 */
int _islower(int c)
{

if (c > 96 && c < 123)
{
return (1);
}
else
{
return (0);
} }
