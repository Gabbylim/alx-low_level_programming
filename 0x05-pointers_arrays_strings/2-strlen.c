
/**
 * _strlen - returns the length of a string
 * @s: its a pointer to a string
 * Return: value of string length
 */

int _strlen(char *s)
{
long int n;

for (n = 0; s[n] != '\0'; n++)
{
;
}
return (n);
}
